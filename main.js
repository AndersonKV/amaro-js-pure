window.addEventListener("load", function () {
  //executa apos carregar toda a pagina
  const template = new Template();
  template.getApi();
  template.handleAddEventListener();

  //new Template().handleErrorImage;
});
const item = [];
var itemCart = [];
